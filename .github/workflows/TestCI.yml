name: Test CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Add a line to Readme
      run: echo Add a line to Readme >> README.md
    - name: See all the Readme contents
      run: cat README.md
